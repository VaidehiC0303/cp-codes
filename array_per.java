import java.util.*;
public class array_per {
    // leetcode 46
    public static void main(String[] args) {
        int[] nums = {1,2,3};
        ArrayList<ArrayList<Integer>> ans = new ArrayList<>();
        <ArrayList<Integer>> temp = new ArrayList<>();
    }
    public static void call(int nums ,ArrayList<ArrayList<Integer>> ans,int i,<ArrayList<Integer>> temp ) {
        for(i=0;i<nums.length;i++){
            temp.add(nums[i]);
            if(nums[i]==temp.contains(num[i])){
                return;
            }
            else {
                temp.add(nums[i]);
            }

        }
        if ()
    }
}
